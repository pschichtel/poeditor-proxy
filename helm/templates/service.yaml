apiVersion: v1
kind: Service
metadata:
  name: {{ include "poeditor-proxy.fullname" . }}
  labels:
    {{- include "poeditor-proxy.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "poeditor-proxy.selectorLabels" . | nindent 4 }}
